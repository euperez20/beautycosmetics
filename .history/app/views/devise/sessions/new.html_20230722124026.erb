<h2>Registro</h2>

<%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
  <%= f.email_field :email, autofocus: true, placeholder: 'Correo electrónico' %>
  <%= f.password_field :password, autocomplete: 'new-password', placeholder: 'Contraseña' %>
  <%= f.password_field :password_confirmation, autocomplete: 'new-password', placeholder: 'Confirmar contraseña' %>
  <%= f.text_field :name, placeholder: 'Nombre' %>
  <%= f.text_field :address, placeholder: 'Dirección' %>
  <%= f.select :province_id, options_for_select(Province.all.map { |province| [province.name, province.id] }), prompt: 'Seleccionar provincia' %>
  <%= f.submit 'Registrarse' %>
<% end %>

<%= render "devise/shared/links" %>
