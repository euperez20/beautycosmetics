<h1>Detalles del elemento del carrito</h1>

<% if @cart_items.any? %>
  <ul>
    <% @cart_items.each do |item| %>
      <li>
        Nombre del producto: <%= item['product_name'] %>
        Cantidad: <%= item['quantity'] %>
        Precio: <%= item['price'] %>
      </li>
    <% end %>
  </ul>
<% else %>
  <p>No hay productos en el carrito.</p>
<% end %>

<p>Customer: <%= session[:user_name] %></p>
<p>Address: <%= session[:user_address] %></p>
<p>Province: <%= Province.find_by(id: session[:province_id])&.name %></p>
<p>Total Af: <%= number_to_currency(session[:total_amount_with_taxes]) %></p>
<p>Total GST: <%= number_to_currency(session[:total_gst]) %></p>

