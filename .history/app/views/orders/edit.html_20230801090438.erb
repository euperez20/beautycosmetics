 <h1>Edit Order</h1>

<%= render "form", order: @order %>



<br>

<div>
  <%= link_to "Show this order", @order %> |
  <%= link_to "Back to orders", orders_path %>
</div> 

<!-- app/views/orders/edit.html.erb 
<div class="container mt-5">
  <div class="row">
    <div class="col-md-6 offset-md-3">
      <h1 class="text-center mb-4">Edit Order</h1>

      <%= form_with(model: @order, url: order_path(@order), method: :put) do |form| %>
        <div class="mb-3">
          <%= form.label :modified_name, "Name" %>
          <%= form.text_field :modified_name, class: "form-control" %>
        </div>

        <div class="mb-3">
          <%= form.label :modified_address, "Address" %>
          <%= form.text_area :modified_address, class: "form-control" %>
        </div>

        <div class="mb-3">
          <%= form.label :modified_province_id, "Province" %>
          <%= form.collection_select :modified_province_id, Province.all, :id, :name, { prompt: true }, class: "form-control" %>
        </div>



        <%= render @order %>

        <%= form.submit "Update Order", class: "btn btn-primary" %>
      <% end %>
    </div>
  </div>
</div>

-->
